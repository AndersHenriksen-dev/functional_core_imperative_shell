# Copy to .env and fill values for local runs.
# Do not commit real secrets.

# Example paths or credentials used via Hydra overrides.
# CUSTOMERS_PATH=/app/data/silver/customers.csv
# TRANSACTIONS_PATH=/app/data/silver/transactions.csv
# DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/db
